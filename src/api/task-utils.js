export function validateTask(task) {
  // TODO: Throw errors for each of the missing task attributes
  return task !== '';
}

export function validateSeries(series) {
  // TODO: Throw errors for each of the missing series attributes
  return series !== '';
}
